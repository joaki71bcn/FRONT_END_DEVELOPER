// ejemplos de funciones arrow sin parametros
// const saludar = () => {
//   console.log("test");
// };
// saludar();

// ejemplos de funciones arrow con parametros
// const saludar2 = (a, b) => {
//   console.log(a,b);
// };
// saludar2(1, 5);

// const multiplicar = (num) => {
//   console.log(num * 2);
// };
// multiplicar(2);

// ejemplo arrow con return - sin llave es un return por lo que hay que almacenar para visualizar
const retorno = (resultado) => resultado ** 3;
visualizar = retorno(3);
console.log(visualizar);


// ejercicio funciones flecha - 1
// Crear y llamar a una función flecha que recibe un número y muestre su cubo con un console.log.
// const cubo = (num1) => {
//   console.log(num1 ** 3);
// };
// cubo(3);

// ejercicio funciones arrow - 2
// Crear y llamar a una función flecha que recibe la velocidad en Km/hora y la devuelva en en metros/hora. Recoger el valor devuelto y mostrarlo con un console.log.
// const kmsHoraConverter = (kmsHora) => kmsHora * 1000;
// console.log(kmsHoraConverter(3));

// ejercicio funciones arrow - 3
// Crea y llamar a una función flecha que recibe el ancho y el alto de un rectángulo y devuelva su área.
// Recoger el valor devuelto y mostrarlo con un console.log.
// Cuando una función recibe varios parámetros, estos irán separados por coma.
// calculateArea(width, height);
// const calculateArea = (width, height) => width * height;
// console.log(calculateArea(5, 7));

// ejercicio funciones arrow - 4
//  Crear y llamar a una función flecha que recibe la base y la altura de un triángulo y devuelva su área.
// Recoger el valor devuelto y mostrarlo con un console.log.
// Área triángulo = base*altura/2
// const triangulo = (base, altura) => base * altura / 2;
// console.log(triangulo(20, 5));

// ejercicio funciones arrow - 5
// Crear estas dos funciones flecha:
// calculatePerimeter(radius); 
// Perímetro de la circunferencia = 2*Math.PI*radio
// calculateArea(radius); 
// Área de la circunferencia = Math.PI*radio*radio
// Recoger los valores devueltos por las funciones y mostrarlos con un console.log.
// const calculatePerimeter = (radius) => 2 * Math.PI * radius;
// console.log(calculatePerimeter(10));
// const calculateArea2 = (radius) => Math.PI ** radius;
// console.log(calculateArea2 (10));





